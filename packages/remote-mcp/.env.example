# hivemind-remote-mcp configuration.
# Copy to .env and fill in. .env is gitignored — never commit real secrets.

# --- Enclave (the confidential recall backend) ---
ENCLAVE_URL=http://localhost:3000
# Local-dev fallback group (used only when auth is OFF). With Stytch auth ON, the
# group comes from each caller's binding, not these.
HIVEMIND_NAMESPACE=
HIVEMIND_ACCOUNT_ID=
# Optional: pin the on-chain-attested enclave pubkey (hex). If unset, the server
# fetches it from the enclave /health_check (fine for the local loop only).
# ENCLAVE_PUBKEY=

# --- Server ---
PORT=8787
# Public base URL of THIS server (the OAuth `resource`). Use the deployed URL in prod.
SERVER_URL=http://localhost:8787

# --- Stytch OAuth (claude.ai Custom Connectors) ---
# Leave STYTCH_DOMAIN blank to run in open local mode (no auth).
# Project domain from Stytch dashboard → Connected Apps (e.g. https://<slug>.customers.stytch.dev).
STYTCH_DOMAIN=
STYTCH_PROJECT_ID=
STYTCH_PUBLIC_TOKEN=
# Management API key — used for per-user namespace lookup (future multi-tenant).
STYTCH_MGMT_KEY_ID=
STYTCH_MGMT_SECRET=

# --- Multi-tenant identity binding ---
# HMAC secret shared with the bot — signs/verifies bind tokens. MUST match the bot.
BIND_SIGNING_SECRET=
# Stytch project SECRET (secret-test-… / secret-live-…) — backend API for reading/
# writing user trusted_metadata (the binding). NOT the public token or mgmt key.
STYTCH_PROJECT_SECRET=
# Stytch backend API base: test → https://test.stytch.com, live → https://api.stytch.com
STYTCH_API_BASE=https://test.stytch.com
